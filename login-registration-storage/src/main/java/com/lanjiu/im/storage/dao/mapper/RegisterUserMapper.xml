<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanjiu.im.storage.dao.mapper.impl.RegisterUserMapper">
    <resultMap id="BaseResultMap" type="com.lanjiu.im.storage.pojo.RegisterUserPO">
        <id column="user_id" property="userId" jdbcType="INTEGER" />
          <result column="user_acount" property="userAcount" jdbcType="VARCHAR"/>
          <result column="password" property="password" jdbcType="VARCHAR"/>
          <result column="mobile_phone_number" property="phone" jdbcType="VARCHAR"/>
          <result column="creation_time" property="date" jdbcType="TIMESTAMP"/>
    </resultMap>
    <sql id="Base_Column_List">
        user_id,user_acount,password,mobile_phone_number,creation_time
    </sql>
    <insert id="insertRegisterUser" parameterType="com.lanjiu.im.storage.pojo.RegisterUserPO">
        insert into registered_user(user_id,user_acount,password,mobile_phone_number,creation_time)
        values(#{userId,jdbcType=INTEGER},#{userAcount,jdbcType=VARCHAR},#{password,jdbcType=VARCHAR},#{phone,jdbcType=VARCHAR},#{date,jdbcType=TIMESTAMP})
    </insert>
    <delete id="deleteRegisterUser">
        delete from registered_user where user_id= #{userId,jdbcType=INTEGER}
    </delete>
    <update id="updateRegisterUser" parameterType="com.lanjiu.im.storage.pojo.RegisterUserPO">
        update registered_user
        <set>
            <if test="userAcount != null">
             user_acount =#{userAcount},
            </if>
           <if test="password !=null and password != ''">
              password =#{password},
            </if>
            <if test="phone != null and phone != '' ">
                mobile_phone_number =#{phone},
            </if>
            <if test="date != null">
              create_time = #{date},
            </if>
        </set>
          WHERE user_id =#{userId}
    </update>

    <select id="selectAllRegisterUser" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from registered_user
    </select>
    <select id="selesctRegisterUser" resultMap="BaseResultMap">
            select
        <include refid="Base_Column_List"/>
            from registered_user
            where user_id = #{userId,jdbcType=INTEGER}
    </select>

    <select id="selectUserPass" parameterType="java.lang.Integer" resultType="java.lang.String">
        select password from registered_user where user_id =#{userId,jdbcType=INTEGER}
    </select>

    <select id="searchUserById" resultMap="BaseResultMap" parameterType="java.lang.Integer">
        select  *  from registered_user where  user_id = #{userId,jdbcType=INTEGER}
    </select>

    <resultMap id="BaseVersionMap" type="com.lanjiu.im.storage.pojo.VersionPO">
        <id column="id" property="id" jdbcType="INTEGER" />
        <result column="phone_type" property="phoneType" jdbcType="INTEGER"/>
        <result column="version_code" property="versionCode" jdbcType="INTEGER"/>
        <result column="version_name" property="versionName" jdbcType="VARCHAR"/>
        <result column="constraint" property="constraint" jdbcType="INTEGER"/>
        <result column="path" property="path" jdbcType="VARCHAR"/>
        <result column="describe" property="describe" jdbcType="VARCHAR"/>
    </resultMap>

    <select id="selectNewVersion" resultMap="BaseVersionMap">
                select  *  from app_version where version_code = (select MAX(version_code) from app_version)
        </select>

    <select id="getUserByPhone" resultMap="BaseResultMap" parameterType="java.lang.String">
        select   *  from  registered_user  where mobile_phone_number = #{phone,jdbcType=VARCHAR}
    </select>

    <resultMap id="baseUserDevice" type="com.lanjiu.im.storage.pojo.UserDevice">
        <id column="user_id" property="user_id" jdbcType="INTEGER"/>
        <result column="factory" property="manufacturer" jdbcType="VARCHAR"/>
        <result column="token" property="push_token" jdbcType="VARCHAR"/>
    </resultMap>
    
    <select id="getUserDevice" resultMap="baseUserDevice" parameterType="java.lang.Integer">
        select * from user_device where user_id =#{userId,jdbcType=INTEGER}
    </select>
    
    <insert id="insertUserDevice" >
        insert into user_device (user_id,factory,token) values (#{userId,jdbcType=INTEGER},#{manufacturer,jdbcType=VARCHAR},#{pushToken,jdbcType=VARCHAR})
    </insert>

    <update id="updateUserDevice" >
        update user_device
        <set>
            <if test="userId != null">
                user_id =#{userId},
            </if>
            <if test="manufacturer !=null and manufacturer != ''">
                factory =#{manufacturer},
            </if>
            <if test="pushToken != null and pushToken != '' ">
                token =#{pushToken},
            </if>
        </set>
        where
          user_id =#{userId,jdbcType=INTEGER}
    </update>

</mapper>