<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lanjiu.im.grpc.dao.mapper.impl.GroupUnusedBeautifulNumberMapper">
    <resultMap id="groupUnusedBeautifulNumberMap" type="com.lanjiu.im.grpc.pojo.GroupUnusedBeautifulNumber">
        <result property="tenMillion" column="ten_million" javaType="java.lang.Integer"></result>
        <result property="oneMillion" column="one_million" javaType="java.lang.Integer"></result>
        <result property="oneHundredThousand" column="one_hundred_thousand" javaType="java.lang.Integer"></result>
        <result property="tenThousand" column="ten_thousand" javaType="java.lang.Integer"></result>
        <result property="oneThousand" column="one_thousand" javaType="java.lang.Integer"></result>
        <result property="oneHundred" column="one_hundred" javaType="java.lang.Integer"></result>
        <result property="ten" column="ten" javaType="java.lang.Integer"></result>
        <result property="one" column="one" javaType="java.lang.Integer"></result>
    </resultMap>

    <select id="selectOnlyGroupUnusedBeautifulNumber" resultMap="groupUnusedBeautifulNumberMap">
		select * from group_unused_beautiful_number limit 1
	</select>

    <select id="selectAllGroupUnusedBeautifulNumber" resultMap="groupUnusedBeautifulNumberMap">
		select * from group_unused_beautiful_number
	</select>

    <select id="selectGroupUnusedBeautifulNumber" resultMap="groupUnusedBeautifulNumberMap">
		select * from group_unused_beautiful_number where ten_million = #{tenMillion} and one_million = #{oneMillion} and one_hundred_thousand = #{oneHundredThousand} and ten_thousand = #{tenThousand}
		and one_thousand = #{oneThousand} and one_hundred = #{oneHundred} and ten = #{ten} and one = #{one}
	</select>

    <select id="selectGroupUnusedBeautifulNumberByThousand" resultMap="groupUnusedBeautifulNumberMap">
		select * from group_unused_beautiful_number where ten_million = #{tenMillion} and one_million = #{oneMillion} and one_hundred_thousand = #{oneHundredThousand} and ten_thousand = #{tenThousand}
		and one_thousand = #{oneThousand}
	</select>

    <insert id="insertGroupUnusedBeautifulNumber" parameterType="com.lanjiu.im.grpc.pojo.GroupUnusedBeautifulNumber">
        insert into group_unused_beautiful_number (ten_million, one_million, one_hundred_thousand, ten_thousand, one_thousand, one_hundred, ten, one)
        values (#{tenMillion,jdbcType=INTEGER}, #{oneMillion,jdbcType=INTEGER}, #{oneHundredThousand,jdbcType=INTEGER},
        #{tenThousand,jdbcType=INTEGER}, #{oneThousand,jdbcType=INTEGER},
        #{oneHundred,jdbcType=INTEGER}, #{ten,jdbcType=INTEGER}, #{one,jdbcType=INTEGER})
    </insert>
    <delete id="deleteGroupUnusedBeautifulNumber" >
		delete from group_unused_beautiful_number where ten_million = #{tenMillion} and one_million = #{oneMillion} and one_hundred_thousand = #{oneHundredThousand} and ten_thousand = #{tenThousand}
		and one_thousand = #{oneThousand} and one_hundred = #{oneHundred} and ten = #{ten} and one = #{one}
	</delete>
</mapper>